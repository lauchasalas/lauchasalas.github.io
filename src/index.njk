---
layout: base.njk
title: "Inicio"
description: "Portfolio — Lautaro Salas Ane"
---

<!-- Hero Section -->
<section
  class="hero-section fade-in"
  style="background-image: url('/photos/hero.jpg')"
>
  <div class="hero-overlay">
    <h1 class="hero-title">Capturando Momentos</h1>
    <p class="hero-subtitle">Fotografía profesional & Marketing visual</p>
    <a href="#portfolio" class="btn-primary">Ver Portfolio</a>
  </div>
</section>

<!-- Sobre mí -->
<section
  id="sobre-mi"
  class="container-custom py-24 text-center fade-in"
  data-aos="fade-up"
>
  <h2>Sobre mí</h2>
  <p class="max-w-3xl mx-auto leading-relaxed">
    Soy Lautaro Salas Ane, fotógrafo y especialista en marketing digital. Mi pasión es capturar momentos auténticos y convertirlos en historias visuales inolvidables.
  </p>
</section>

<!-- Mini-Carrusel Horizontal -->
<section
  id="portfolio"
  class="fade-in"
  data-aos="fade-right"
>
  <h2 class="container-custom text-3xl font-bold mb-6">Explora mis Fotos</h2>
  <div class="mini-carousel">
    {% for photo in collections.photo %}
      <img src="{{ photo.url }}" alt="{{ photo.data.title }}" loading="lazy" />
    {% endfor %}
  </div>
</section>

<!-- Grid de Miniaturas -->
<section
  id="galeria"
  class="fade-in"
  data-aos="fade-up"
>
  <h2 class="container-custom text-3xl font-bold mb-6">Galería Completa</h2>
  <div class="thumb-grid">
    {% for photo in collections.photo %}
      <a href="{{ photo.url }}" data-lightbox="gallery" data-title="{{ photo.data.title }}">
        <img src="{{ photo.url }}" alt="{{ photo.data.title }}" loading="lazy" />
      </a>
    {% endfor %}
  </div>
</section>

<!-- Contacto -->
<section
  id="contacto"
  class="container-custom py-24 text-center fade-in"
  data-aos="fade-up"
>
  <h2 class="text-3xl md:text-4xl font-bold mb-4">Contacto</h2>
  <p class="mb-6">¿Tienes un proyecto en mente? Escríbeme:</p>
  <a href="mailto:tuemail@ejemplo.com" class="btn-primary">Enviar mensaje</a>
</section>
